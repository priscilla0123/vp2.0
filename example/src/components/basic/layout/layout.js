import {
    vpLayout,
    vpHeader,
    vpSide,
    vpContent,
    vpFooter
} from '../../../../../dist/vp';

export default {
    components: {
        vpLayout,
        vpHeader,
        vpSide,
        vpContent,
        vpFooter
    }
}